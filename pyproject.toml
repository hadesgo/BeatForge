[project]
name = "beatforge"
version = "0.2.0"
description = "Local-AI music video editor for consumer NVIDIA GPUs"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "librosa>=0.11.0",
  "numpy>=2.0",
  "pillow>=11.0",
  "pydantic>=2.10",
  "rich>=13.9",
  "soundfile>=0.13",
  "typer>=0.15",
]

[project.optional-dependencies]
ai = [
  "accelerate>=1.5",
  "faster-whisper>=1.1",
  "huggingface-hub>=0.29",
  "modelscope>=1.22",
  "sentencepiece>=0.2",
  "transformers>=5.16.1",
]
ai-cpu = ["torch==2.14.0", "torchvision==0.29.0", "torchaudio==2.11.0", "torchcodec==0.16.0"]
ai-cuda = ["torch==2.14.0", "torchvision==0.29.0", "torchaudio==2.11.0", "torchcodec==0.16.0", "bitsandbytes>=0.50.2"]
ai-cuda126 = ["torch==2.14.0", "torchvision==0.29.0", "torchaudio==2.11.0", "torchcodec==0.16.0", "bitsandbytes>=0.50.2"]
qwen = [
  "qwen-vl-utils>=0.0.14",
  "sentence-transformers[image]>=5.7",
]
music-ai = ["all-in-one-infer>=3.0", "beat-this"]

[dependency-groups]
dev = ["pytest>=8.3"]

[project.scripts]
beatforge = "beatforge.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
default-groups = ["dev"]
conflicts = [
  [{ extra = "ai-cpu" }, { extra = "ai-cuda" }],
  [{ extra = "ai-cpu" }, { extra = "ai-cuda126" }],
  [{ extra = "ai-cuda" }, { extra = "ai-cuda126" }],
]

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", extra = "ai-cpu" },
  { index = "pytorch-cu130", extra = "ai-cuda" },
  { index = "pytorch-cu126", extra = "ai-cuda126" },
]
torchvision = [
  { index = "pytorch-cpu", extra = "ai-cpu" },
  { index = "pytorch-cu130", extra = "ai-cuda" },
  { index = "pytorch-cu126", extra = "ai-cuda126" },
]
torchaudio = [
  { index = "pytorch-cpu", extra = "ai-cpu" },
  { index = "pytorch-cu130", extra = "ai-cuda" },
  { index = "pytorch-cu126", extra = "ai-cuda126" },
]
torchcodec = [
  { index = "pytorch-cpu", extra = "ai-cpu" },
  { index = "pytorch-cu130", extra = "ai-cuda" },
  { index = "pytorch-cu126", extra = "ai-cuda126" },
]

[tool.pytest.ini_options]
testpaths = ["tests"]
