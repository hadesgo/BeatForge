# 2026-09-17

## MoviePy 方向作废（上午，未落地）
- 用户曾要求"渲染模块改为 MoviePy 驱动、取消 ffmpeg 命令行"。做了可行性评估后，用户改口放弃，
  仓库回滚到 `6c851bd`，MoviePy 依赖与 `beatforge/render/` 包均已清除，`uv sync` 已还原 pillow 12.3.0。
- 评估结论（以后若再被问到可直接引用）：MoviePy 本身不渲染，它是 ffmpeg 的 Python 包装
  （`imageio-ffmpeg` 自带二进制），所以"取消 ffmpeg 命令行"只能做到"不再自己拼命令行"。
  真正的代价是能力净减：只有 `CrossFadeIn` 一个转场（现库有 58 个）、字幕退回 PIL 文本（丢掉 libass 的
  逐字 `\k` 高亮与字体回退）、`perspective` 逐帧运镜要改成逐帧 PIL 仿射，且每帧多一次 numpy 往返。

## 特效扩充：动效 / 转场 / 字幕（下午）
用户需求：「加入更多的动效、转场，以及字幕特效。参考剪映的特效」。三项全部扩完，**225 passed**。

### 单图运镜 12 → 21 种
新增 9 种，全部复用同一次 `perspective`（不叠加第二次重采样）：
- `whip_pan` 甩镜（`tmix` 沿路径混邻近帧做拖影，否则快摇读起来像跳切）
- `spiral_in` 推进 + 滚转、`roll_drift` 荷兰角漂移
- `handheld` 手持晃动（正弦叠加，按 `on/fps` 计时，所以长镜头上频率不会变慢）
- `tilt3d_back/front/left/right` 3D 转向（梯形变形）
- `pulse_in` 一拍推两下

**核心机制改动（重要，见 MEMORY.md）**：`_camera_expressions` + `_perspective_filter` 重构成
`_camera_quad`（返回 8 个角点表达式）+ `_perspective_filter(quad)`，四边形由**同一个中心点 + 同一个半尺寸**
推导，再整体旋转（roll）或做梯形变形（keystone）。这样滚转和 3D 转向都是单次 `perspective`。
代价：可平移量程必须按四边形的**包围盒**算而不是裁剪框——`_required_zoom` 会把整条 zoom 曲线抬高到
足够容纳包围盒（抬高整条而不是夹住某几帧，保住原本的推进幅度）。

### 转场 14 族/39 名 → 21 族/58 名 + 3 种效果型转场
- 补齐 xfade 剩余家族：`mask`(circlecrop/rectcrop)、`open`/`close`(vert/horz open/close)、
  `smooth`、`corner`(wipetl/tr/bl/br)、`wind`(hlwind…)、`soft`(distance/fadeslow)。
- **效果型转场**（`_EFFECT_TRANSITIONS`）：`glitch`、`light_leak`、`film_burn`。
  它们不用 xfade，滤镜直接烧进两个镜头各自的帧里，时间线上是硬切——冲击发生在切点**上**而不是横跨切点。
  为此把 `_transition_spec` 的返回值从 `(name, seconds)` 改成 `_Transition(kind, name, seconds)`，
  带 `.handle`（只有 `xfade` 需要额外 footage 补重叠）。
- **闪光的时序坑**：`fade` 在 `st+d` 才到达目标色，所以出点一侧要从 `duration - flash - 1/fps` 起算。
  停在片段末尾的话最后一帧只走到三成，闪光退化成一记轻微提亮（实测 peak 只比 rest 高 1.1）。

### 字幕特效 6 → 16 种
新增 10 种：`punch`、`slide`、`flip_in`、`neon`、`neon_flicker`、`shake`、`wave`、`rainbow`、
`spotlight`、`glitch`。按剪映式"入场/持续/异类"三类组织，全部用 ASS 标签实现。
- `_subtitle_effect(name, line, ...) -> (prefix, text)` 取代原来的 if/elif 链；
  `_per_character(text, tag_for)` 负责逐字特效的 stagger。
- `SUBTITLE_EFFECTS` 成为唯一真源：`config.SubtitleEffectChoice` 与
  `ai_director.SubtitleEffect` 都用 `Literal[*SUBTITLE_EFFECTS]` 生成，避免两处手抄名单走偏。
- 导演的逐句选择阶梯重写：副歌高能量走冲击组、旋律句走流动组、暗黑情绪走 glitch/spotlight/typewriter。

### 新增探针（三个失效都是静默的，必须靠探针兜）
- `scripts/quad_probe.py`：逐帧求值全部运镜的四边形，报越界像素与形变量。调滚转/梯形强度先跑它。
- `scripts/cut_effect_probe.py`：在真实切点上渲染三种效果型转场，测**均值亮度 + 空间标准差 + 暖度**。
  只测平均亮度是不够的——噪点和通道分离几乎不改变均值，会看起来"什么都没做"（第一版就踩了这个）。
- `scripts/subtitle_effect_probe.py`：libass 会**静默忽略**不认识的标签，所以逐帧比对确认每种动效真的在动，
  同时查墨量确认真的画出来了。`--sheet` 可输出接触表。

### 顺手修掉的既有问题
- `tests/test_planner.py` 里 `test_planned_effects_are_all_names_the_renderer_can_build` 与
  `test_planned_transition_families_are_all_known_to_the_renderer` **各重复定义了两遍**，删掉后一份。
- `scripts/effects_preview.py` 原来 `shutil.rmtree(输出目录)`，被安全守卫拦下（目录里已有 4399 个文件）；
  改成只清理它自己创建的 `clips/`、`frames/`。

### 文档与产物
- README 补：9 种新运镜、四边形包围盒与 `_required_zoom` 的说明、转场表（21 族 + 3 效果型）、
  16 种字幕特效的分类说明、三个新探针、配置表 `subtitle_effect` 行。
- 产物：`.probe/effects/effects-preview.png`（28 种画面效果接触表）、
  `.probe/subtitle/subtitle-preview.png`（16 种字幕特效接触表）。

## 收尾复查：又挖出两个真 bug（同日晚些）
三项扩充做完后回头核对"新东西是否真的被用上"，挖出两个各自独立的真 bug：

### 1. `whip_pan` 甩不起来（位移只有 8px）
可平移量程正比于 zoom：pan 位移 = `(W - W/zoom) × 比例`。原配置 zoom_to=0.045，
整段位移只有 8px @640，无论名义上多快都只是慢漂移。改成 zoom_to=0.42 后位移 46px。
`drag` 的权重核也改成按帧数生成的三角核（`min(i+1, n-i)`），5 帧即 `1 2 3 2 1`。
隔离验证 `tmix` 确实生效：同一段平移，相邻帧差 22.42（无）→ 21.23（3 帧）→ 20.40（5 帧），
拖影把相邻帧桥接起来，这正是运动模糊该有的效果。
（注意：直接看边缘能量会被 `perspective` 的重采样干扰——像素级噪点测试卡一重采样就掉，
测不出 tmix 本身。要用**结构化**测试卡 + 相邻帧差。）

### 2. 运镜轮转被多图合成"饿死"（13/21 种）
`_choose_image_effect` 的合成门控和 `_single_image_effect` 的轮转**共用全局 `index`**。
被合成吃掉的序号，其轮转槽位就永远轮不到：同一首歌的冲击段 9 个镜头里，
非合成的只有序号 18/21/23（余数 3、1、3），槽位 0（`punch_in`）和 2（`whip_pan`）全部落空。
改成 `create_plan` 维护 `single_image_cursor`（`_choose_image_effect` 返回 `layer_count == 0`
时递增），轮转按 cursor 推进。同一首歌从 13 种提升到**用满 21 种**。
- 教训：单个样本歌曲测不出这类饿死——覆盖率取决于该曲的段落和能量分布。第一次扫描时
  我的合成歌曲副歌能量刚好卡在 `.65` 边界，导致 4 个冲击运镜全没选中，误判成"分支够不到"。
  已加 `test_every_camera_move_is_reachable_from_some_song` 跨 28 首歌曲扫描。
- 顺带删掉 `tests/test_planner.py` 里**重复定义第二遍**的 `_varied_song`。

### 新增守门测试
- `test_the_whip_pan_travels_far_enough_to_be_a_whip`：位移 > 5% 画宽 + 图里有 `tmix`。
- `test_a_drag_is_only_asked_for_by_moves_that_need_one`：`tmix` 只允许出现在 `whip_pan` 上
  （它要额外帧缓冲且会软化画面，挂在慢推镜上只会让画面显脏）。
- `test_every_camera_move_is_reachable_from_some_song`：跨歌曲扫全部 21 种运镜。
- 全量 **228 passed**。

### 3. 转场族同样被"相位饿死"（4 个族永远轮不到）
同一类问题的第二种成因。转场的轮转原本按**镜头序号** `index % N`，而转场分支很窄
（`open` 只在 dreamy 歌曲的段落切换处出现，一首歌只有两三次），几次触发可能全落在同一余数上。
实测 `close`/`open`/`radial` 三个族在整个扫描里出现 0 次。

修法：`_assign_transitions` 维护 `visible`（已分配的**可见**转场数），轮转按它推进；
密度门控仍用镜头序号 `index`——两者回答不同问题，所以 `_transition_family` 现在同时接收
`index` 和 `visible`，且改成关键字参数（两个 int 挨着太容易传错）。
- 效果：`light_leak` 从 0 变成可达；`visible` 让同一分支的连续触发取连续槽位，
  分支触发次数 ≥ 轮转长度时就能覆盖全组。
- **可达性还取决于歌曲本身**：第一版扫描用固定段落布局，副歌边界能量恒 >.78，
  被 `flash` 分支抢占，所以 `radial` 永远轮不到——那不是代码 bug，是样本太窄。
  用随机化歌曲（不同段落布局 + 能量曲线）重扫后 **24 族全部可达**。
- 补 `test_every_transition_family_is_reachable_from_some_song`：用**穷举找出**的 8 组
  (段落布局, 能量形状, 情绪) 组合覆盖全部 24 族（单首固定歌曲只覆盖约一半）。
  这 8 组是脚本扫出来的最小覆盖面，不是手猜的。

全量 **230 passed**（35s；一次 70s 的读数是系统负载，重跑即恢复）。

## 视觉检索：把素材压到编码器预算内（傍晚）
用户需求：「WeMM-Embedding + Qwen3-VL Reranker 的时候，将素材压缩至合理范围内，
以减少对显存的占用和提高效率」。

### 问题
Qwen-VL 系 processor 在视觉塔之前会自己把图缩到 `max_pixels = 1280*28*28` ≈ 1.0MP。
所以把 24MP 原图交给它**换不来任何模型能看到的细节**，只换来全分辨率解码 + 全分辨率缩放 +
两者同时在内存里。而原实现有两处直接喂原始分辨率：
1. **图片素材以原始文件路径喂模型**（`str(asset.file)` / `{"image": str(asset.file)}`），
   编码和重排各一次；
2. **重排阶段对每个候选都重新解码原图**——同一张素材在候选表里通常出现好几次，
   浪费是乘以候选数的。
另外视频抽帧固定 `scale=768:-2`，对竖屏失效（768x2048 仍是 1.5MP）。

### 改动
- `AIConfig.vision_input_pixels`（默认 `1280*28*28`）。
- `_fit_within(w, h, max_pixels)`：按**像素预算**而不是长边缩放（长边规则的失效模式正是竖屏），
  只缩不放，并取偶（yuv420p 与 patch 网格都要偶数）。
- `_model_image(asset)`：图片素材的预算内缓存副本（`cache/model-input/`），
  编码与重排共用；本来就在预算内的素材**原样透传**，连缓存目录都不建。
  存副本时用 Pillow 的 `source.draft("RGB", target)` 走 JPEG 的 1/2、1/4、1/8 免解码降采样，
  大文件连全分辨率解码都省掉。
- `_video_frame_samples` 改为按素材实际尺寸算出预算内尺寸，并把该尺寸写进缓存 key
  （否则改预算会静默复用旧尺寸的帧）。
- `_video_frame_at(asset, count, target_time)`：重排只回读它真正需要的那一帧。
- `similarities` 在聚合循环里用完关键帧后立刻把 `spans[position]` 的 frames 置 None，
  不再把整套采样帧持有到整轮结束（重排原本还要再拿一次）。
- SigLIP2 回退路径也走 `_load_bounded_image`。
- `VisionIndex.input_pixels` 设成**类属性**，这样用 `__new__` 绕过构造器的测试仍有合理默认值。

### 实测（`scripts/vision_input_probe.py`，两条路径各起一个子进程，峰值读数才各归各的）
8 张 6000x4000 照片：耗时 1.29s → 0.80s；峰值内存 224 MiB → 61 MiB；
**送进模型的像素 192.00 MP → 8.02 MP（少 23.9x）**。
最后一项才是显存那本账上的数字：视觉编码器的 patch 数量和激活显存直接按像素数走，
而且重排阶段要乘以候选数。
- 写这个探针时踩到：Windows 上 `ctypes` 调 `GetProcessMemoryInfo` **必须显式声明
  `argtypes`/`restype`**，否则不报错、直接返回 0（第一版读数就是 0 MiB）。
  另外 `resource` 模块是 Unix-only，得用 `ctypes` 走 Windows 分支。

全量 **233 passed**。

## 字幕融入画面（参考官方歌词 MV）
用户需求：「有没有可能，将字幕融入到画面中。并且做出一些效果，比如这个MV一样」，
并给了参考片 `利比《跳楼机》(官方歌词MV)`。用户明确说**不需要英文译文**。

### 先看参考片做了什么（`ffmpeg` 抽帧 + tile 网格 + 连续帧序列）
参考片不用底部字幕条。抽 2fps 连续帧后确认的手法：
1. **按演唱位置切句**：一句歌词拆成分片，摆在画面不同位置；
2. **每片在自己被唱到的时刻淡入**（连续帧里"那天的天气"先出现、"难得 放晴"后出现，两者同时停留，
   下一句起来时上一片还在）——这是它读起来"在画面里"的核心；
3. **主动避开主体**：字落在人形之外的空处，有时上下夹持（主体在中间）、有时对角、有时同基线拉开间距；
4. **衬线细体、纯白、无描边**；
5. 器乐段不出字。

关键判断：**不是按字数平分**。最初按字数中点切，得到"黎明照 / 亮天空"——把"照亮"劈开了。

### 实现
- `lyrics.py` 新增 `Placement`（text/x/y/align/tokens/start）+ `plan_placements()`：
  断点取**逐字时间轴里最长的静音**（`_MIN_BREAK_SECONDS = 0.22`）→ 退到标点 → 都没有则**整句不拆**。
  落点用 `shot.focus_point` 避让：主体偏上整组下压、偏下上抬，只有居中才用完整版式。
  三种版式：`sandwich`（上下夹持）/ `diagonal`（对角）/ `gap`（同基线拉开）。
- `_fragment_entrance()`：每片 `\alpha&HFF&` + `\t(delay, delay+260, \alpha&H00&)`，
  delay 由 `placement.start - line.start` 算出。**必须追加在特效标签之后**——libass 按顺序应用
  `\t` 链，后写的对同一属性胜出。同时把特效自带的 `\fad(in,out)` 的入场半边改成 `\fad(0,out)`，
  否则两个 alpha 动画会互相打架。
- 自由版式下 `karaoke` 不再逐字扫光（`_karaoke_fragment` 直接返回纯文本）：分片已经承载了时间信息，
  再扫一遍是把同一件事说两遍。`band` 版式保留扫光。
- `renderer.py` 新增 `_subtitle_placements()` + `_lyric_focus_points()`（按时间一次走完，不重复搜镜头）。
- 配置：`subtitle_layout`（默认 `free`）、`subtitle_outline`（默认 1.1）、`subtitle_fill`（默认 `solid`）。

### 镂空填充（`subtitle_fill = "knockout"`）
"融入画面"的字面做法：没有字幕层，只有画面里一个字形空洞，字中透出同一帧提亮虚化的样子。
- 遮罩 = 同一份歌词脚本渲染成白字黑底，`format=gray` 后用 `alphamerge` 取亮度当 alpha，
  所以脚本里任何动画（淡入/缩放/分片延迟）都会让空洞同步跟随。
- **踩到的坑**：只提亮填充是不够的。自由版式故意把字放在画面空处，那里平滑而暗，
  实测文字对比度是 **−6.2**（比周围还暗）。改成**同时把画面整体压暗**（`_KNOCKOUT_LIFT=0.16`,
  `_KNOCKOUT_DIM=-0.10`）后对比度变成 **+47 / +10**，成为滤镜图的固有属性而不是碰巧。
- `_knockout_graph(subtitle, seconds, cfg)` 的时长改成**参数传入**，不在里面调 ffprobe——测试里
  没有真实视频文件，而且这样职责更清楚。

### 新增探针与测试
- `scripts/subtitle_layout_probe.py`：**不依赖 LRC**（自由排版要的"歌手在哪换气"LRC 带不了），
  直接构造带逐字时间轴的歌词走真实 `render()`，把每个镜头三帧铺成接触表。
  `--layout free|band`、`--fill solid|knockout`。
- 测试：断句落在停顿处（不是中点）、无停顿不拆、标点拆分、避让主体、分片延迟出场、
  band 版式不变、描边可配、镂空图的对比度由构造保证、镂空真的渲染出来且与 solid 不同。

### 顺手修的
- 追加测试块时 heredoc 被执行了两次，`tests/test_lyrics.py` 和 `tests/test_renderer.py`
  各多出一整份重复定义。已删掉，并跑了一遍全仓 `def`/`class` 重复检查确认干净。
- `_fit_font_size` 里的"视觉宽度"计算与新增的 `_visual_units` 重复，已合并到后者。

全量 **242 passed**。

## 剪辑风格：把专家决策结构化为可选配置
用户需求：「我们想创作MV视频内容，但剪辑技术和创意需要提升，请短视频剪辑教练帮我们提升短视频制作水平。
将剪辑专家的技术和创意都结构化为可选配置」。当前会话挂的 Expert 是 `ShortVideoEditingCoach`。

### 设计判断
**剪辑不是"look"**。调色/字体/颗粒属于艺术指导，读的是同一首歌；这一层只管**关于时间的决定**：
镜长、切点网格、转场音量、景别对比、运镜强度。所以新建了 `beatforge/editing.py`，
和 `director.py`（艺术指导）、`planner.py`（编排）分开。

八种风格各自是**一套自洽的习惯**，不是一堆独立旋钮——挑一半"卡点"再挑一半"长镜"，
剪出来就是没有观点的片子。`edit_style = auto` 按情绪选；`manual` 表示"我手工设了值，别接管"。

### 新增能力（不是只加配置入口）
1. **`cut_alignment` 切点网格**：拍 / 小节线 / 乐句 / 歌词行。原本只有 downbeats。
   这是整个文件里最听得出来的一个决定。
   **乐句网格必须按风格自己的镜长窗口自适应取几小节**——固定四小节在 120bpm 下是 8 秒，
   而镜长上限 6 秒的风格永远找不到候选点，会静默退化成按小节切，字段就成了装饰。
2. **`section_speedup` 段落内加速**：越接近本段顶点镜头越短。
3. **`transition_flavour` 转场取向**：含蓄/均衡/冲击，各自一套内部转场池 + 降级替代表。
4. **`shot_size_contrast`**：原本写死 -0.09，现在由风格给出（0.08~0.22）。

### 踩到 / 修掉的三个真问题
1. **`transition_density` 几乎不起作用**：`impact`/`breathe` 两个意图分支在密度门控**之前**就返回了，
   所以密度压不住它们——一个只想要 10% 可见转场的纪实风格实测仍有 47%。把门控提到意图分支之前。
2. **`_quieten` 只在一个分支里调用**，所以含蓄风格的结构性转场仍然会出现闪白/故障。改成所有出口统一过滤。
3. **风格给了 `transition_density`，但被 `create_plan` 的默认值 0.35 盖住**——只有调用方显式传才生效。
   改成"风格接管它表过态的项"（镜长、转场密度、多图比例），其余仍走参数。这是同一个病：
   **配置被接受但没生效**，这一轮已经是第二次（上次是 `_KNOCKOUT_LIFT`）。
4. `montage` 原本用 `phrase`，但它的镜长上限 3.6s 装不下一小节，等于该字段对它无效 → 改回 `downbeat`；
   `dream` 和 `minimal` 原本都落在同一个乐句网格上、中位镜长都是 6.00s → 把 `minimal` 拉到 4.0–9.0s。

### 验证
`scripts/edit_style_probe.py`：把同一首歌喂给全部八种风格，打印镜头数/镜长/可见转场/转场音量/
景别撞车率。实测镜头中位 **0.50s（冲击碎剪）~ 8.00s（极简留白），差 16 倍**；
可见转场 **10%（纪实）~ 70%（冲击）**；含蓄风格冲击转场占比 **0%**。
写探针时踩到：素材景别按索引交替的话，**所有风格都会 100% 换景别**，指标失去意义——
改成**成块分布**（每 7 个素材同景别）才测得出 `shot_size_contrast`（实测同景别相邻 34% → 3%）。

### 测试（`tests/test_editing.py`，20 条）
每个风格自洽（tempo 在自己区间内、枚举值合法）、风格之间不塌成同一个（镜数/中位镜长/可见转场三元指纹）、
auto 的映射与"响而密的歌不该选长镜风格"、四种切点网格各自落在正确的拍点上、
段落加速真的改变切点、景别对比真的减少撞车、风格真的接管手工镜长、
含蓄风格不出现冲击转场、冲击风格允许、密度真的控制可见转场比例。

全量 **262 passed**。

## 转录前的人声分离（MelBand-RoFormer）
用户需求：「在进行asr之前使用MelBand-RoFormer进行人声和伴奏分离，将人声拿去ASR转录」。

### 设计判断
- **只给人声轨做转录，但强制对齐也用同一条人声轨**。对齐问的是"这个词是什么时候唱的"，
  没有鼓压着好回答得多。
- **拍点/能量/段落分析仍跑整轨混音**——那些要的就是鼓，人声轨里没有鼓。所以分离的作用域
  严格限定在 ASR 阶段，`speech_source()` 是唯一入口。
- 结果按「源文件 + 模型」缓存（`cache/<stem>-<digest>-vocals.wav`）。**模型名必须进缓存 key**：
  两个检查点给出两条不同的人声轨，混用在下游完全看不出来，只会表现成歌词略有不同。
- 装不上时**退回整轨并明确说明**。降级本身没问题，悄悄降级才是问题——整轨转录出来的是
  另一份、更差的歌词，而下游没有任何环节能分辨。`plan.json` 的 `models.separation` 会记录。

### 依赖
新增 `separation` extra（`audio-separator>=0.47`）。它只依赖 PyTorch——RoFormer 检查点走
torch 而不是 ONNX，**不需要 onnxruntime**。torch 约束 `<3,>=2.3` 与项目的 2.14 兼容。

### 选模型：用包自带的基准数据，不靠猜
`audio-separator` 自带 `models-scores.json`（115 个模型的人声/伴奏 SDR 实测）。
按人声 SDR 排序的 MelBand-RoFormer：
| 平均 SDR | 最高 | 检查点 |
|---|---|---|
| 11.53 | 15.57 | `mel_band_roformer_kim_ft_unwa.ckpt` |
| **11.49** | **15.63** | **`vocals_mel_band_roformer.ckpt`（选为默认）** |
| 10.02 | 16.13 | `model_mel_band_roformer_ep_3005_sdr_11.4360.ckpt` |
默认值与前两名在噪声范围内持平，取最通用的那个。卡拉 OK 模型主轨是伴奏、平均 SDR 明显更低。

### 核对 API 时发现的坑（读源码，不靠文档猜）
- `Separator.separate()` **返回的是完整路径**，不是裸文件名（文档写的是 "Fully written output paths"）。
  我原来的 `work / name` 靠 `Path` 的绝对路径语义侥幸能用，已改成显式判断 `is_absolute()` 并补测试。
- 选轨**必须按名字匹配 `vocals`**，不能按列表位置：换个命名习惯的检查点会把伴奏轨喂给 ASR，
  转录出来是空的。

### 顺手修的真问题
`release_gpu()` 只 `except ImportError`。torch 存在但不可用（半装完、没有 `cuda` 属性、
驱动拒绝初始化）时会抛 `AttributeError` 把调用它的阶段带崩。它是**尽力而为的清理**，
不该成为失败源，已放宽到 `(ImportError, AttributeError, RuntimeError, OSError)`。
（这个是在等 torch 安装的中间态里真实撞到的。）

### 测试（`tests/test_separator.py`，11 条）
缓存 key 含模型名、缓存命中不跑模型、按名字而非位置选人声轨（含顺序反转）、
找不到人声轨时报错而不是猜、用假 Separator 跑通完整流程（加载→分离→保留人声→清理临时目录）、
缺包时提示装哪个 extra、流水线拿到人声轨、可关闭、缺依赖时退回混音并说明原因、默认开启。

### 验证状态（重要：未完成的部分）
- **代码层面已验证**：273 个测试通过；`tests/test_separator.py` 11 条覆盖缓存 key、选轨、
  缺包降级、完整流程（用假 Separator）。
- **真实模型未跑**：`uv sync --extra ai --extra ai-cpu --extra separation` 跑了 35 分钟仍未装完
  （torch CPU 轮子解压极慢，10 分钟只从 51M 到 54M，疑似被杀软扫描），用户指示不再等待，已中止。
  所以 **"分离后人声轨里确实是人声、ASR 转录确实变好" 这一条没有实测数据**。
- 已备好验证脚本 `scripts/vocal_separation_probe.py`：用**频段能量分布**判断分离是否真的发生
  （整轨混音大量能量在 <200Hz 的鼓和贝斯，人声轨几乎没有；人声集中在 200Hz–4kHz）。
  该判据已在合成信号上自检通过（混音 69% 低频 / 人声 100% 人声段）。
  用法：`uv run python scripts/vocal_separation_probe.py --audio <歌或视频> --seconds 90`，
  会打印频段占比并输出 20 秒 A/B 试听片段。
- **中止安装后必须清理 venv**：被 kill 的 `uv sync` 会留下**没有 dist-info 的孤儿目录**
  （`torch`/`functorch`/`onnx`/`onnx2torch`），`uv sync` 认不出来不会删。
  危险之处是 `import torch` **会成功**（空命名空间包）而 `torch.__version__` 不存在，
  于是 `transcriber.py` 的 `try: import torch` 放行、后面用 `torch.bfloat16` 时才炸，报错点离原因很远。
  识别方法：目录存在、有/无 `__init__.py`、但 `ls site-packages/<pkg>-*.dist-info` 为空。
  注意 `_distutils_hack` 属于 setuptools、`PIL`/`sklearn` 的 dist-info 名字不同（Pillow/scikit_learn），
  不能按"目录名 == dist-info 名"一刀切。

### doctor 命令的两个健壮性问题（中止安装的副产品，但都是真 bug）
在等依赖安装时顺手给 `doctor` 加了人声分离的检查行，结果发现它自己会崩：
1. **torch 存在但不可用**时 `torch.cuda.is_available()` 抛 `AttributeError`，doctor 直接吐 traceback。
   doctor 存在的意义就是报告环境问题，**它绝不该是环境问题弄崩的那个命令**。已加
   `except (AttributeError, RuntimeError, OSError)` 分支，报告"PyTorch 损坏 + 重新 uv sync"。
2. **`importlib.metadata.version("transformers")` 返回 `None`**（半写状态的 dist-info：
   目录在、METADATA 为空），随后 `.split(".")` 抛 `AttributeError`。已抽出
   `_supports_native_asr(version)` 处理 `None`、预发布版号（`5.16.1rc1`）和垃圾串，
   一律回答"未就绪"而不是崩。
3. 新增"人声分离"检查行：`importlib.util.find_spec("audio_separator")`。

### 新增测试
- `test_vocal_separation_is_its_own_extra`：separation 必须是独立 extra、不含 onnxruntime、
  不被 ai extra 带上（否则只想跑 ASR 的机器要白装一个分离器）。
- `test_native_asr_version_check_survives_a_broken_environment`：`None` / 预发布版号 / 垃圾串。
- `test_release_gpu_survives_an_unusable_torch`：`import torch` 成功但没有 `cuda` 属性时不得抛异常。

全量 **276 passed**。

## 把人声分离模型加进下载步骤
用户需求：「将 vocals_mel_band_roformer 模型，添加到下载步骤里面」。

### 关键点：它不是仓库快照
其他模型都是 HF/ModelScope 的 repo id，走 `snapshot_download`；分离检查点是
`audio-separator` 自己目录里的**单个 .ckpt**，要另走一条路。
该包提供了**只下载不加载**的入口 `Separator.download_model_files(filename)`
（返回 `(filename, type, friendly_name, path, yaml)`），不需要显存也不需要先把模型读进内存。
`ModelRequirement` 因此加了 `provider` 字段（`snapshot` / `audio-separator`）。

### 最关键的一条不变式：下载目录 == 运行时查找目录
原来分离器找的是 `cache/separator-models`，而下载器会放到 `cache/models/...` —— **不一致**。
不一致的后果是静默的：下载步骤看起来成功，运行时却找不到文件、首次使用重新拉一遍，没有任何地方报错。
现在两边都从 `separator.SEPARATOR_SUBDIR` 派生，并有一条测试专门断言这条等式。

### 默认体验问题（差点写错）
`separate_vocals` 默认是 `true`，所以如果分离模型下载失败就让整条命令失败，
**默认配置会在所有没装 separation extra 的机器上跑不通**。所以区分两件事：
- **没装包** → `progress("skipped", ...)`，打印"跳过 + 缺哪个 extra"，其余模型照常下载；
- **真的下载失败** → 照旧进 `failures`，最后抛 `ModelDownloadError`。

### 测试（`tests/test_model_downloader.py` +5 条）
分离模型进清单、关掉 `separate_vocals` 就移出清单、
**下载目录与运行时目录一致**、用假 Separator 验证走的是 `download_model_files`（只下载不加载）、
缺 extra 时是"跳过"而不是"失败"且其余模型仍被下载。

全量 **281 passed**。

## 把人声分离模型加进下载步骤
用户需求：「将 vocals_mel_band_roformer 模型，添加到下载步骤里面」。

### 关键点：它不是仓库快照
其他模型都是 HF/ModelScope 的 repo id，走 `snapshot_download`；分离检查点是
`audio-separator` 自己目录里的**单个 .ckpt**，要另走一条路。
该包提供了**只下载不加载**的入口 `Separator.download_model_files(filename)`
（返回 `(filename, type, friendly_name, path, yaml)`），不需要显存也不需要先把模型读进内存。
`ModelRequirement` 因此加了 `provider` 字段（`snapshot` / `audio-separator`）。

### 最关键的一条不变式：下载目录 == 运行时查找目录
原来分离器找的是 `cache/separator-models`，而下载器会放到 `cache/models/...` —— **不一致**。
不一致的后果是静默的：下载步骤看起来成功，运行时却找不到文件、首次使用重新拉一遍，没有任何地方报错。
现在两边都从 `separator.SEPARATOR_SUBDIR` 派生，并有一条测试专门断言这条等式。

### 默认体验问题（差点写错）
`separate_vocals` 默认是 `true`，所以如果分离模型下载失败就让整条命令失败，
**默认配置会在所有没装 separation extra 的机器上跑不通**。所以区分两件事：
- **没装包** → `progress("skipped", ...)`，打印"跳过 + 缺哪个 extra"，其余模型照常下载；
- **真的下载失败** → 照旧进 `failures`，最后抛 `ModelDownloadError`。

### 测试（`tests/test_model_downloader.py` +5 条）
分离模型进清单、关掉 `separate_vocals` 就移出清单、
**下载目录与运行时目录一致**、用假 Separator 验证走的是 `download_model_files`（只下载不加载）、
缺 extra 时是"跳过"而不是"失败"且其余模型仍被下载。

全量 **281 passed**。

## 人声分离的端到端实跑与两个真 bug（依赖装好之后）
用户装好依赖后实测。这一步终于拿到了真实数据，也抓到了两个问题。

### 依赖：我判断错了一个
我在 README 里写过"RoFormer 走 torch 不需要 onnxruntime"——**错了**。
`audio_separator/separator/separator.py` 第 24 行 `import onnxruntime as ort` 是**顶层无条件导入**。
另外 `uvr_lib_v5/spec_utils.py` 顶层 `import audioread`，而 **audioread 根本没写进该包的依赖表**。
两个都补进了 `separation` extra。教训：**别按架构推测依赖，读源码或直接 import 一遍**。
（当时还顺手写了一条断言"onnxruntime 不在 extra 里"的测试，已改成断言它在。）

### 真 bug：选轨按子串匹配，选中了伴奏
该包的输出命名是 `<输入>_(<分句>)_<模型>.wav`：
```
clip_(vocals)_vocals_mel_band_roformer.wav        ← 人声
clip_(instrumental)_vocals_mel_band_roformer.wav  ← 伴奏
```
默认检查点叫 `vocals_mel_band_roformer`，所以**每个输出文件名都含 "vocals"**。
原来的 `any(hint in path.name for hint in ("vocals","vocal"))` 等于没筛，取列表里第一个。
第一次实跑就中招：缓存下来的"人声轨"**低频占 52.5%（比原始混音的 49.2% 还高）**，
而真正的人声（低频 25.6%）被留在了工作目录里。
症状是**转录出一片空白**，看起来像模型坏了而不是选轨错了——典型的静默失败。
修复：按 `_\(([^)]+)\)_` 提取**分句标记**，只认 `vocals/vocal/voice/lead`；
认不出标记时退而求其次"排除已知的伴奏名"，而不是按位置猜。
**原来的测试之所以没抓到**：它用的文件名是 `song_(Vocals)_model.wav`，模型名里没有 "vocals"。
已全部改成真实命名，并断言两种返回顺序都选对。

### 修复后实测（利比《跳楼机》官方 MV，45 秒，CPU）
| 频段 | 原始混音 | 人声轨 |
|---|---|---|
| 低频 <200Hz | 49.2% | **25.6%** |
| 人声 0.2–4kHz | 50.2% | **70.8%** |
| 高频 >4kHz | 0.6% | 3.6% |

25.6% / 70.8% 与之前那份"残留文件"的数字完全一致，反证了之前确实选错了。
模型 913MB，CPU 推理 **204 秒 / 分钟音频**（45 秒用了 153 秒）。

### 顺手修掉：测试会联网下载 913MB
分离依赖装上之后，`download_required_models` 的测试开始真的走分离分支去**联网下载模型**。
按该文件已有的做法加了可注入的 `separator_download_fn`（与 `snapshot_download_fn` 对称），
测试用桩替代。**"测试不该联网"这条不能破。**

全量 **281 passed**。
